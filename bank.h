struct Account {
    int accNo;        // whole number
    char name[50];    // text (string in C = array of characters)
    char type[10];    // "Savings" or "Current"
    float balance;    // decimal number
};